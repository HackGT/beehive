git:
  remote: https://github.com/hackgt/helpq
  branch: dev

wants:
  mongo: true

files:
  /etc/helpq:
    secret: true
    contents:
      settings.json:
        env: SETTINGS_FILE

env:
  ROOT_URL: https://helpq.teeny.hack.gt

