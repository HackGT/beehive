domain: hack.gt
apps:
  - name: helpq
    git: https://github.com/HackGT/helpq.git
    branch: dev
    run:
        port: 3000
    env:
        - name: MONGO_URL
          value: mongodb://main-mongo-mongodb
  - name: main-mongo
    helm: mongodb
    version: 0.4.4
