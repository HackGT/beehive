apiVersion: v1
kind: Secret
metadata:
  name: git-<%= app['git']['slog'].tr '/', '-' %>-secrets
type: Opaque
data:
<% app['secrets']&.each do |name| %>
  <%# Fills the secret with a sentinel value, real secrets are uploaded by hand. %>
  <%= name %>: MA==
<% end %>
