apiVersion: v1
kind: ConfigMap
metadata:
  name: <%= "#{app_name}" %>
  labels:
    name: <%= "#{app_name}" %>
    heritage: Biodomes
data:
  <% app['files']&.each do |key, file| %>
  <%= file['key'] %>: !!str <%= file['contents'].inspect %>
  <% end %>
