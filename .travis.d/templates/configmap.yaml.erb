apiVersion: v1
kind: ConfigMap
metadata:
  name: <%= "#{app_name}" %>
  labels:
    name: <%= "#{app_name}" %>
data:
  <% app['files']&.each do |key, file| %>
  <%= file['path'] %>: !!str <%= file['contents'].inspect %>
  <% end %>
