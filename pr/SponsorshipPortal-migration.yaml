git:
    remote: "https://github.com/hackgt/sponsorshipportal.git"
    branch: "migration"

secrets-source: git-hackgt-sponsorshipportal-secrets
deployment:
    replicas: 1
    strategy:
        type: recreate

env:
    DB_CONNECTIONSTRING: "postgres://sponsorshipportal:LfwA8vQR38Fw@postgres-postgresql/sponsorshipportal-dev?sslmode=disable"

