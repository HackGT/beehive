git:
    remote: "https://github.com/hackgt/scheduler.git"
    branch: "bug-missing-return"

secrets-source: git-hackgt-scheduler-secrets
deployment:
    replicas: 1
    strategy:
        type: recreate
