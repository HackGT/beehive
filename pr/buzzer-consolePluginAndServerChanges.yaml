git:
    remote: "https://github.com/hackgt/buzzer.git"
    branch: "consolePluginAndServerChanges"

secrets-source: git-hackgt-buzzer-secrets
deployment:
    replicas: 1
    strategy:
        type: Recreate
