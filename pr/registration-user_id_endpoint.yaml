git:
    remote: "https://github.com/hackgt/registration.git"
    branch: "user_id_endpoint"

secrets-source: git-hackgt-registration-secrets
deployment:
    replicas: 1
    strategy:
        type: recreate
