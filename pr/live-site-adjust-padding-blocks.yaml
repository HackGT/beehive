git:
    remote: "https://github.com/hackgt/live-site.git"
    branch: "adjust-padding-blocks"

secrets-source: git-hackgt-live-site-secrets
deployment:
    replicas: 1
    strategy:
        type: recreate
