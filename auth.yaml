git: https://github.com/HackGT/cas-gradle-overlay-template.git
deployment:
  memory: '256Mi'
files:
  /etc/cas/config:
    secret: true
    contents:
      adminusers.properties: yes
      cas.properties: yes